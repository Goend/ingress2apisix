# APISIX 单节点性能测试报告

| 项目 | 内容 |
|---|---|
| 报告日期 | 2026-09-02 |
| 测试对象 | `nj-eks1318-k8s2apisix-2` / `10.31.2.14:9080` |
| HTTPS 入口 | `10.31.2.14:9443` |
| 后端位置 | `nj-eks1318-k8s2slave-5`，CNI Pod IP `10.31.3.38:8080` |
| 客户端位置 | `nj-eks1318-k8s2slave-6`，客户端 Pod `10.31.2.21` |
| 测试范围 | 单 APISIX 节点；不包含多节点聚合能力 |

## 1. 执行摘要

本报告评估单个 APISIX 数据面实例在指定客户拓扑下的 HTTP 反向代理能力。主场景使用精确 `20,480 bytes` 的 GET 响应和精确 `20,480 bytes` 的 POST 请求体，客户端、APISIX 和后端分别固定在 `slave-6`、`apisix-2` 和 `slave-5`，以减少测试端之间的资源竞争。

### 结论

- **20KB GET 推荐稳定工作点：约 `30,000 TPS`。** 60 秒窗口实际 `29,695.43 TPS`，完成 `1,781,748` 个请求，错误率 `0%`，P99 `10.43ms`，P99.9 `32.08ms`，最大 `114.11ms`。
- **20KB POST 推荐稳定工作点：约 `30,000 TPS`。** 60 秒窗口实际 `29,693.44 TPS`，完成 `1,781,602` 个请求，错误率 `0%`，P99 `49.95ms`，P99.9 `67.65ms`，最大 `162.30ms`。
- **GET 拐点：约 `40,000–45,000 TPS`。** 固定速率从 `40k` 提升到 `45k` 后，实际吞吐仅由 `38,645.99` 提升到 `42,572.40 TPS`，P99 从 `381.44ms` 增至 `3.18s`。
- **POST 拐点：约 `35,000–36,000 TPS`。** 固定速率从 `35k` 提升到 `36k` 后，实际吞吐由 `33,762.81` 提升到 `34,461.00 TPS`，P99 从 `435.71ms` 增至 `1.65s`。
- **首要瓶颈是 APISIX 数据面计算与双向 TCP/CNI 转发处理能力。** APISIX 容器无 CPU quota，但实际 CPU 集合为 `0–27`，可用 `28` 个逻辑 CPU；高负载窗口中 APISIX CPU、TCP 重传和排队长尾同步上升。
- **后端未出现 cgroup CPU throttling。** 后端 Pod 为 `10C request / 12C limit`，全部正式窗口 `nr_throttled=0`；后端在高负载 POST 时接近 `12C`，属于次级压力因素，但不是 APISIX 长尾突变的唯一原因。

本报告以“无压测错误、60 秒稳定、P99 不进入秒级排队”为容量建议依据；推荐容量不是协议理论上限，也不代表所有业务插件、TLS 证书、日志和真实业务处理逻辑下的容量。

## 2. 测试范围与判定方法

### 2.1 覆盖指标

- 20KB GET/POST 吞吐阶梯、延迟分位数、实际完成请求数、错误率；
- 60 秒稳定工作点；
- 响应体大小矩阵：空响应、1KB、10KB、20KB、100KB；
- 并发连接矩阵：`100/500/1000/2000/5000`；
- HTTP keep-alive、HTTP 短连接、HTTPS keep-alive、HTTPS 短连接；
- 路由未命中和现有 `ingress2apisix` 入口补充检查；
- 后端 Pod 直连对照；
- CPU、内存、cgroup throttling、活跃 TCP、TIME_WAIT、conntrack、文件描述符、网卡字节、TCP 重传、软中断和 Pod 状态。

### 2.2 测试参数

- 固定速率工具：`wrk2`，`8 threads`、`2048 connections`、`20s` 正式窗口、`20s` 请求超时；
- 稳定性窗口：固定 `30k TPS`，`60s`，`8 threads`、`2048 connections`；
- 并发和入口补充：`wrk 4.2.0` 开放式压测，`8 threads`，对应连接数，`10s`；
- 固定速率测试使用 `Host: apisix-perf-standard.test`；HTTPS 使用临时自签名证书，客户端跳过证书校验；
- GET 接口返回精确 `20,480 bytes`；POST 使用精确 `20,480 bytes` 请求体，后端完整接收后返回 `ok`。

## 3. 环境与拓扑

|角色|对象|网络/调度信息|资源配置|
|---|---|---|---|
|APISIX|`apisix-hxqn7`|Pod IP `10.31.2.14`；`hostNetwork=true`；节点 `nj-eks1318-k8s2apisix-2`|`worker_processes=32`；CPU quota unlimited；cpuset `0–27`|
|后端|`apisix-perf-standard-backend-7d8f45bd8f-pblzs`|CNI IP `10.31.3.38`；节点 `nj-eks1318-k8s2slave-5`；Service `10.222.219.65:80`|`10C request / 12C limit`；Nginx `worker_processes=12`|
|客户端|`apisix-perf-standard-client`|Pod IP `10.31.2.21`；`hostNetwork=true`；节点 `nj-eks1318-k8s2slave-6`|`4C request / 8C limit`|

集群版本为 `v1.31.8-es`，节点操作系统为 Kylin Linux Advanced Server V11，内核为 `6.6.0-32.7.v2505.ky11.x86_64`，APISIX 镜像为 `hub.easystack.io/multiarch/apisix:3.16.0-es`，OpenResty 版本为 `1.27.1.2`。

### 3.1 流量拓扑

```text
客户端 Pod/slave-6  10.31.2.21
        │ 20KB HTTP/HTTPS 请求
        ▼
APISIX-2 / hostNetwork  10.31.2.14:9080/9443
        │ APISIX VM enp3s0；上游走 CNI
        ▼
后端 Pod/slave-5  10.31.3.38:8080
```

APISIX 的 `enp3s0` 计数对应 APISIX-2 虚拟机的宿主网络接口；后端的 `eth0` 计数对应后端 Pod 的 CNI 容器网卡；客户端由于采用 `hostNetwork`，其 `enp3s0` 计数对应客户端节点网络命名空间。

## 4. APISIX 与后端配置

|配置项|值|
|---|---|
|APISIX worker|`worker_processes: 32`；实际 cpuset `0–27`|
|连接参数|`worker_connections: 10620`；`enable_reuseport: true`；`enable_http2: true`|
|代理缓冲|`proxy_request_buffering off`；`proxy_buffer_size 512k`；`proxy_buffers 8 1024k`|
|超时|`proxy_read_timeout 300s`|
|日志|access log 输出 `/dev/stdout`；后端 Nginx `access_log off`|
|后端处理|POST 路径缓冲请求体并转发至本地 sink，返回 `ok`|
|文件描述符|APISIX 进程 `Max open files=1,048,576`|

### 4.1 测试接口有效载荷

|接口|方法|请求体|响应体|用途|
|---|---|---|---|---|
|`/healthz`|GET|—|2 bytes|健康检查|
|`/get0`|GET|—|0 bytes / 204|空响应基线|
|`/get1k`|GET|—|1,024 bytes|小响应|
|`/get10k`|GET|—|10,240 bytes|中等响应|
|`/get20k`|GET|—|20,480 bytes|主 GET 场景|
|`/get100k`|GET|—|102,400 bytes|大响应|
|`/post20k`|POST|20,480 bytes|2 bytes|主 POST 场景|

## 5. 20KB GET 性能结果

以下为固定速率、keep-alive、`8 threads/2048 connections/20s` 结果。P95/P99/P99.9/最大延迟均为客户端测得的端到端请求延迟。

|目标速率|实际 TPS|完成请求|错误率|P50|P90|P95|P99|P99.9|最大|评价|
|---|---|---|---|---|---|---|---|---|---|---|
|5,000|4878.50|97576|0.0000%|2.65ms|3.64ms|4.25ms|6.66ms|9.94ms|17.92ms|基线|
|10,000|9729.85|194603|0.0000%|2.77ms|3.65ms|3.96ms|4.72ms|9.80ms|45.25ms|稳定|
|20,000|19360.62|387183|0.0000%|2.62ms|4.01ms|4.78ms|8.19ms|13.57ms|21.77ms|稳定|
|30,000|29083.64|581699|0.0000%|2.95ms|5.22ms|6.61ms|11.97ms|26.03ms|49.41ms|推荐|
|35,000|33836.36|676706|0.0000%|3.78ms|10.44ms|15.51ms|37.18ms|103.42ms|166.14ms|可探测|
|40,000|38645.99|772942|0.0000%|5.78ms|39.78ms|138.50ms|381.44ms|457.47ms|541.70ms|长尾开始|
|45,000|42572.40|851477|0.0000%|20.80ms|1.57s|2.33s|3.18s|3.45s|3.78s|拐点后|
|50,000|43755.19|875131|0.0000%|34.75ms|3.93s|5.05s|6.61s|7.05s|7.49s|严重排队|

### 5.1 GET 60 秒稳定性

|目标速率|实际 TPS|完成请求|错误率|P50|P90|P95|P99|P99.9|最大|结论|
|---|---|---|---|---|---|---|---|---|---|---|
|30,000|29695.43|1781748|0.0000%|3.01ms|5.13ms|6.27ms|10.43ms|32.08ms|114.11ms|通过稳定性窗口|

GET 在 `30k` 稳定窗口内保持零 socket 错误和零非 2xx；`35k` 仍未出现请求错误，但 P99.9 已达到约 `103ms`，`40k` 时 P99 达到 `381.44ms`，`45k` 以后进入秒级排队。

## 6. 20KB POST 性能结果

POST 的 TPS 是精确 `20,480 bytes` 请求体下的事务速率，不是仅发送请求头的 TPS。后端端点在完整接收请求体后返回 `ok`。

|目标速率|实际 TPS|完成请求|错误率|P50|P90|P95|P99|P99.9|最大|评价|
|---|---|---|---|---|---|---|---|---|---|---|
|5,000|4867.59|97355|0.0000%|3.15ms|9.10ms|15.13ms|27.77ms|45.15ms|56.67ms|基线|
|10,000|9729.72|194588|0.0000%|3.01ms|3.94ms|4.27ms|5.13ms|7.55ms|14.62ms|稳定|
|20,000|19408.38|388137|0.0000%|3.43ms|5.11ms|5.83ms|7.53ms|13.95ms|66.69ms|稳定|
|25,000|24244.68|484855|0.0000%|3.37ms|5.47ms|6.72ms|19.22ms|132.35ms|205.05ms|稳定|
|30,000|29086.54|581684|0.0000%|4.32ms|9.10ms|12.69ms|27.66ms|80.32ms|124.35ms|推荐|
|35,000|33762.81|675220|0.0000%|9.45ms|52.64ms|107.90ms|435.71ms|667.65ms|836.61ms|拐点前缘|
|36,000|34461.00|689150|0.0000%|9.90ms|900.09ms|1.20s|1.65s|2.10s|2.53s|拐点|
|40,000|36720.57|734658|0.0000%|16.53ms|2.29s|3.54s|8.76s|9.37s|9.76s|严重排队|

### 6.1 POST 60 秒稳定性

|目标速率|实际 TPS|完成请求|错误率|P50|P90|P95|P99|P99.9|最大|结论|
|---|---|---|---|---|---|---|---|---|---|---|
|30,000|29693.44|1781602|0.0000%|5.01ms|29.63ms|40.48ms|49.95ms|67.65ms|162.30ms|通过稳定性窗口|

POST 在 `35k→36k` 出现明显长尾突变：实际吞吐只增加约 `698 TPS`，P90 从 `52.64ms` 增至 `900.09ms`，P99 从 `435.71ms` 增至 `1.65s`。这定义了本拓扑下 POST 的主要容量拐点。

## 7. 响应体大小矩阵

固定目标 `5,000 TPS`、`12s`、keep-alive；以下响应体长度已经通过接口校验。

|接口|响应体|实际 TPS|错误率|P50|P90|P99|P99.9|最大|APISIX CPU(C)|APISIX RX/TX(Gbps)|
|---|---|---|---|---|---|---|---|---|---|---|
|`/get0`|0B|4743.39|0.0000%|2.09ms|2.82ms|3.82ms|6.34ms|7.47ms|2.23|0.01/0.02|
|`/get1k`|1,024B|4768.68|0.0000%|2.19ms|2.94ms|3.85ms|5.11ms|5.73ms|2.59|0.06/0.07|
|`/get10k`|10,240B|4820.14|0.0000%|2.33ms|3.02ms|3.73ms|4.82ms|5.65ms|2.62|0.42/0.43|
|`/get20k`|20,480B|4741.10|0.0000%|2.87ms|4.74ms|8.81ms|10.40ms|11.77ms|2.79|0.79/0.79|
|`/get100k`|102,400B|4819.53|0.0000%|3.48ms|4.43ms|5.78ms|8.29ms|10.79ms|3.52|3.98/3.98|

在 `5k TPS` 下，响应体从空响应增加到 `100KB`，实际 TPS 仍保持在约 `4.74–4.82k`，但 APISIX 双向带宽从约 `0.01Gbps` 增至约 `3.98Gbps`；CPU 仍未接近可用计算池上限，说明该低速矩阵主要用于确认有效载荷和基础带宽行为。

## 8. 并发连接矩阵

并发矩阵使用 `wrk` 开放式压测，固定 `/get20k`、`10s`；开放式工具不提供 HdrHistogram 的 P95/P99.9，本节以工具输出的平均、P99 和最大延迟为主。

|连接数|实际 TPS|平均延迟|P99|最大|错误率|APISIX CPU(C)|TCP inuse 最大|TIME_WAIT 最大|
|---|---|---|---|---|---|---|---|---|
|100|38585.89|2.49ms|—|20.12ms|0.0000%|18.83|370|1486|
|500|52020.80|9.79ms|—|83.54ms|0.0000%|26.39|1105|1637|
|1000|50680.38|19.96ms|—|153.61ms|0.0000%|26.91|1991|1639|
|2000|49289.54|40.51ms|—|178.57ms|0.0000%|26.77|3761|590|
|5000|43871.46|115.41ms|—|929.84ms|0.0000%|25.82|9035|474|

并发数从 `100` 增加到 `500–1000` 时吞吐达到约 `50–52k TPS`，但 APISIX CPU 已达到约 `26–27C`；连接数继续增加到 `5000` 后，吞吐降至约 `43.87k TPS`，平均延迟升至 `115.41ms`，最大延迟约 `929.84ms`。高并发不等价于稳定容量，连接数应与目标延迟和连接生命周期共同规划。

## 9. HTTP、HTTPS 与短连接

|场景|工具/条件|实际 TPS|错误率|P50|P90|P95|P99|P99.9|最大|
|---|---|---|---|---|---|---|---|---|---|
|HTTP GET keep-alive|wrk2，5k/12s|4741.10|0.0000%|2.87ms|4.74ms|6.41ms|8.81ms|10.40ms|11.77ms|
|HTTP GET short|wrk2，5k/12s，Connection: close|4819.94|0.0000%|3.95ms|4.96ms|5.37ms|6.29ms|7.34ms|9.61ms|
|HTTP POST short|wrk2，5k/12s，Connection: close|4818.80|0.0000%|4.28ms|5.64ms|6.20ms|7.54ms|10.37ms|11.95ms|
|HTTPS GET keep-alive|wrk2，5k/12s|4581.36|0.0000%|17.47ms|38.75ms|41.63ms|47.39ms|53.69ms|62.46ms|
|HTTPS POST keep-alive|wrk2，5k/12s|4578.32|0.0000%|23.39ms|42.91ms|47.39ms|55.62ms|61.85ms|64.86ms|
|HTTPS GET short|wrk2，2k/10s，Connection: close|1968.32|0.0000%|3.79ms|4.82ms|5.21ms|6.65ms|19.58ms|25.84ms|
|HTTPS GET open|wrk，1000 connections/10s|23133.30|0.0000%|—|—|—|—|—|183.74ms|

TLS 协商为 TLS 1.3，密码套件为 `TLS_AES_256_GCM_SHA384`；证书为临时自签名证书，客户端明确跳过校验。HTTPS keep-alive 在 `5k TPS` 下无压测错误，但 P50 相比 HTTP 20KB GET 增加到 `17.47ms`，应将 TLS 实际证书链、握手频率、密码套件和连接复用策略纳入生产容量验证。

## 10. 路由与真实入口检查

|场景|请求|实际 TPS|返回结果|错误率|P50|P90|P99|最大|说明|
|---|---|---|---|---|---|---|---|---|---|
|路由未命中|Host `apisix-perf-standard.test` + `/__perf_not_found__`|114804.93|404|100.0000%|—|—|—|116.66ms|APISIX 本地路由处理能力，不代表代理吞吐|
|现有入口|Host `ingress2apisix` + `/`|63575.10|503|100.0000%|—|—|—|83.09ms|Service 当前无可用 Endpoint，不能作为业务链路容量|

现有 `ingress2apisix` Service 的 EndpointSlice 没有可用地址，因此真实入口检查得到 `503`；该结果用于记录入口可用性，不与纯 APISIX→测试后端代理结果混合。

## 11. 后端直连对照

客户端直接访问 `10.31.3.38:8080`，不经过 APISIX，用于区分后端/CNI 链路和代理数据面开销。

|场景|实际 TPS|错误率|P50|P90|P95|P99|P99.9|最大|后端 CPU(C)|后端 RX/TX(Gbps)|
|---|---|---|---|---|---|---|---|---|---|---|
|直连 GET 20KB|28498.34|0.0000%|5.81ms|362.24ms|396.03ms|550.40ms|910.85ms|959.49ms|4.68|0.05/4.85|
|直连 POST 20KB|28448.29|0.0000%|6.50ms|28.43ms|34.27ms|43.33ms|48.45ms|66.50ms|7.54|4.81/0.07|

直连结果表明后端和 CNI 路径本身可以完成约 `28.45–28.50k TPS` 的 20KB 事务；APISIX 路径的主要额外成本来自请求接收、响应转发、连接管理、日志和双向数据面处理。直连对照不用于替代 APISIX 代理容量结论。

## 12. 资源、网络与稳定性证据

### 12.1 关键吞吐点资源窗口

|场景|角色|平均 CPU(C)|最大内存(MB)|RX(Gbps)|TX(Gbps)|TCP 重传增量|throttled 次数|TCP inuse 最大|TIME_WAIT 最大|
|---|---|---|---|---|---|---|---|---|---|
|get20k_30k|apisix|15.41|1150.45|4.91|4.95|187|0|2748|157|
|get20k_30k|backend|4.53|365.16|0.08|4.92|2561|0|567|936|
|get20k_30k|client|4.27|34.25|4.97|0.05|0|0|2086|6249|
|get20k_40k|apisix|20.86|1161.12|6.55|6.59|1163|0|3505|161|
|get20k_40k|backend|6.49|364.65|0.11|6.56|44004|0|1317|1370|
|get20k_40k|client|5.30|34.23|6.53|0.06|0|0|2087|6053|
|get20k_45k|apisix|23.27|1165.19|7.24|7.28|3732|0|3580|159|
|get20k_45k|backend|7.45|364.91|0.12|7.23|58081|0|1392|1552|
|get20k_45k|client|5.47|34.09|7.20|0.07|0|0|2087|5791|
|post20k_30k|apisix|16.79|1157.27|4.94|5.00|281|0|3036|638|
|post20k_30k|backend|7.80|359.30|4.93|0.07|0|0|1080|2471|
|post20k_30k|client|4.89|75.53|0.10|4.92|13805|0|2087|6116|
|post20k_35k|apisix|19.80|1173.54|5.65|5.72|9286|0|4151|179|
|post20k_35k|backend|9.02|370.41|5.59|0.08|1|0|4844|2356|
|post20k_35k|client|5.04|75.29|0.11|5.54|53241|0|2087|6059|
|post20k_36k|apisix|19.87|1161.48|5.81|5.88|1549|0|3871|170|
|post20k_36k|backend|9.16|370.29|5.84|0.08|1|0|3563|2932|
|post20k_36k|client|5.40|75.80|0.11|5.79|50333|0|2087|5547|
|post20k_40k|apisix|21.33|1176.42|6.20|6.27|5936|0|4126|163|
|post20k_40k|backend|9.80|371.27|6.17|0.09|8|0|4666|3297|
|post20k_40k|client|5.09|75.76|0.12|6.09|75383|0|2087|5130|
|stability_get20k_30k_60s|apisix|15.56|1173.48|5.02|5.06|761|0|2595|201|
|stability_get20k_30k_60s|backend|4.35|356.74|0.08|4.80|6115|0|418|1509|
|stability_get20k_30k_60s|client|4.06|33.37|4.64|0.04|0|0|2087|2175|
|stability_post20k_30k_60s|apisix|17.20|1176.86|5.05|5.11|9736|0|4218|212|
|stability_post20k_30k_60s|backend|8.28|381.44|5.02|0.07|1|0|5623|3181|
|stability_post20k_30k_60s|client|4.80|74.93|0.10|4.98|54152|0|2087|2167|

资源字段说明：CPU 为测试窗口内 cgroup CPU 使用量折算的平均核数；内存为窗口内最大观测值；RX/TX 为对应网络命名空间接口字节计数增量折算的平均速率；TCP 重传为 `/proc/net/snmp` 中 `RetransSegs` 累计计数增量。

### 12.2 cgroup 与系统状态

- APISIX `cpu.cfs_quota_us=-1`，`cpuset=0–27`，没有 CPU quota throttle；高负载时 CPU 使用量和网络处理开销持续上升。
- 后端 `cpu.cfs_quota_us=1,200,000`，对应 `12C` limit；正式窗口 `nr_throttled=0`、`throttled_time=0`。
- 客户端 `cpu.cfs_quota_us=800,000`，对应 `8C` limit；HTTPS 开放式场景出现少量客户端 cgroup throttle，固定速率主场景未出现 throttle。
- APISIX 进程 `Max open files=1,048,576`；诊断时 `/proc/1/fd` 为 `172`，测试窗口的最大 TCP inuse 由 `/proc/net/sockstat` 采集，最高约 `9,035`，尚未接近 fd 上限。
- conntrack 诊断值为 `229/2,097,152`（count/max）；没有 conntrack 容量耗尽证据。
- APISIX `enp3s0` 累计硬件错误为 `0`，累计 drop 为 `25`；由于容器镜像不包含 `ip/tc/ethtool/ss/vmstat`，无法从该容器直接取得每个队列的 qdisc drop、网卡硬件队列统计和完整 `vmstat/mpstat` 时间序列。
- `/proc/net/softnet_stat` 可读但未观察到可用于证明软网卡丢包的明确窗口级结论；网络瓶颈判断主要依据 cgroup CPU、吞吐平台、TCP 重传和延迟排队共同判定。

### 12.3 Pod 与节点稳定性

- APISIX Pod、后端 Pod、客户端 Pod 在测试结束状态均为 `1/1 Running`，重启数均为 `0`。
- APISIX-2 节点在测试结束采样约 `2%` 节点 CPU，后端节点约 `7%`，客户端节点约 `8%`；该值是采样时刻的节点总量，不替代测试窗口内的 Pod cgroup 指标。
- 测试窗口中未发现客户端 socket error、非 2xx 业务响应或 Pod 重启；TCP 重传计数上升表示网络命名空间累计重传事件，不等价于应用请求失败。

## 13. 瓶颈定位

### 13.1 主瓶颈：APISIX 数据面计算池

APISIX 没有 CFS CPU quota，但 Pod 被限制在 `0–27` CPU 集合。随着固定速率提高，APISIX cgroup CPU 与 P99/P99.9 延迟同步恶化：GET 在 `40k` 开始出现数百毫秒长尾，`45k` 进入秒级；POST 在 `35k→36k` 出现更早、更明显的长尾跃迁。

### 13.2 放大因素：20KB 双向转发与 TCP/CNI

GET 需要处理客户端到 APISIX、APISIX 到后端、后端返回到 APISIX、APISIX 返回客户端的双向数据路径；POST 还包含 20KB 请求体接收、转发和后端缓冲。高负载窗口中 APISIX `enp3s0` 双向吞吐达到约 `6–8Gbps`，TCP 重传和 TIME_WAIT/活跃连接同步升高，排队延迟因此被放大。

### 13.3 后端是次级压力，不是唯一根因

后端在 POST `40k` 窗口约 `9.80C`，接近 `12C` limit，但 cgroup throttle 为零；后端直连 POST 仍能完成约 `28.45k TPS` 且 P99 约 `43.33ms`。因此，增加后端 CPU 配额不能单独消除 APISIX 侧的长尾，需同时优化 APISIX 数据面计算和网络转发。

### 13.4 可疑的额外开销：APISIX access log

当前 APISIX access log 输出到 `/dev/stdout`，而测试后端关闭 access log。本报告未进行“关闭 APISIX access log”的 A/B，因此不能量化其独立贡献；生产压测优化时应优先做该 A/B。

## 14. 容量建议

|场景|建议稳定工作点|拐点观察|不建议作为稳定容量的区间|
|---|---|---|---|
|20KB GET|`30k TPS`|`40k–45k TPS`|`45k TPS` 以上出现秒级长尾|
|20KB POST|`30k TPS`|`35k–36k TPS`|`36k TPS` 以上出现秒级长尾|
|20KB HTTPS GET/POST|需单独按 TLS 连接模型核算|本次 `5k TPS` keep-alive 无错误|短连接成本显著高于 keep-alive|

建议按以下顺序优化和复核：

1. 关闭或异步化 APISIX access log，保持客户端、后端和路由完全一致，比较 `30k/35k/40k` 三个点；
2. 确认 APISIX Pod 的 CPU 集合是否可以覆盖配置的 `32 workers`，检查 CPU 亲和性、NUMA、virtio RSS/RPS/XPS 和软中断分布；
3. 针对 `20KB POST` 检查请求体缓冲、代理缓冲、TCP autotuning、socket backlog 和 upstream keepalive；
4. 对 HTTPS 分别测量 keep-alive、短连接和真实证书链，避免用自签名证书结果代表生产 TLS 容量；
5. 将推荐容量按 P99、P99.9、错误率和持续时间共同验收，不以瞬时峰值 TPS 作为容量承诺。

## 15. 限制条件

- 本报告只代表 `nj-eks1318-k8s2apisix-2` 单节点、当前 APISIX 配置、当前 CNI 路径和当前后端测试实现；不代表三节点总能力。
- 后端是轻量 Nginx/sink，不包含真实业务 CPU、数据库、序列化、鉴权或插件开销；真实业务应单独建立业务等价压测模型。
- APISIX 镜像中缺少 `ss/ip/tc/ethtool/vmstat/mpstat`，系统级队列、软中断和 guest steal 的完整窗口级时间序列无法从容器内直接获得；报告使用 `/proc`、cgroup、网卡字节和 Kubernetes 指标补足。
- `wrk` 开放式并发矩阵与 `wrk2` 固定速率矩阵不是同一负载模型，结果用于不同维度分析，不应横向直接比较所有指标。
- 现有 `ingress2apisix` Service 没有可用 Endpoint，因此其 `503` 只说明入口后端不可用，不作为业务转发性能结果。
- 测试使用临时自签名 TLS 证书并跳过客户端校验；证书校验、证书链长度和生产 cipher 配置可能改变 HTTPS 结果。

## 16. 原始数据与复核

正式原始数据和采集文件位于：

- `/opt/reports/apisix-performance-2026-09-02-raw-final/`
- 固定速率结果：`*.wrk2.txt`；开放式结果：`*.wrk.txt`；
- 资源采样：`metrics.tsv`；60 秒稳定性采样：`stability-metrics.tsv`；
- 环境与诊断：`environment.txt`、`final-diagnostics.txt`、`extra-diagnostics.txt`、`final-state.txt`；
- 结构化解析：`report-results.json`、`report-windows.json`。

本报告生成时已确认测试 Pod 状态为 Ready、重启数为零，并在测试资源清理前完成原始文件落盘。

## 17. 结论 
目前由于邮储 带宽和cpu性能 物理硬件更好 因此在此基础上 满足三节点10万TPS 应该没有太大问题
